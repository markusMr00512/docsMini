{
  "name": "docsmini-collaboration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "ensure-client": "bun -e \"const fs=require('fs');const p=require('path');const target=p.resolve('node_modules/@prisma/client');const src=p.resolve('../../node_modules/@prisma/client');if(!fs.existsSync(p.join(src,'index.js'))){console.error('[collab] parent @prisma/client not found — run bun install in project root first');process.exit(1)}console.log('[collab] using shared @prisma/client from',src)\""
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "bun-types": "^1.3.4",
    "typescript": "^5"
  }
}
